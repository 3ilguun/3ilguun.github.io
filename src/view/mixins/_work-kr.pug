mixin work(title, subTitle, fileName, imgCount, href)
  .work
    - let baseURL = '../../images/' + fileName + '/' + fileName

    if imgCount > 1
      .work__images
        - for (let x = 1; x < imgCount; x++)
          - let URL = baseURL + '-' + x + '.jpg'
          - let URLxs = baseURL + '-' + x + '-xs.jpg'
          - let URLmd = baseURL + '-' + x + '-md.jpg'
          - let URLsm = baseURL + '-' + x + '-sm.jpg'
          .work__image
            img.lazy(
              src= URLxs
              data-src= URL
              data-srcset= URL + ' 2000w,' + URLmd + ' 1260w,' + URLsm + ' 720w'
            )
    else if imgCount == 1
      .work__image
        - let URL = baseURL + '.jpg'
        - let URLxs = baseURL + '-xs.jpg'
        - let URLmd = baseURL + '-md.jpg'
        - let URLsm = baseURL + '-sm.jpg'
        img.lazy(
          src= URLxs
          data-src= URL
          data-srcset= URL + ' 2000w,' + URLmd + ' 1260w,' + URLsm + ' 720w'
        )

    block

    .work__header
      h2.work__title= title
      p.work__type= subTitle
      if href
        a.work__link(href=href target="_blank")
          span= href
          include ../../images/link.svg


mixin work-grid(fileName, imgCount)
  .work__images-grid
    - for (let x = 1; x < imgCount; x++)
      - let baseURL = '../../images/' + fileName + '/' + fileName + '-' + x
      - let URL = baseURL + '.jpg'
      - let URLxs = baseURL + '-xs.jpg'
      - let URLmd = baseURL + '-md.jpg'
      - let URLsm = baseURL + '-sm.jpg'
      .work__image
        img.lazy(
          src= URL
          data-src= URL
          data-srcset= URL + ' 2000w,' + URLmd + ' 1260w,' + URLsm + ' 720w'
        )